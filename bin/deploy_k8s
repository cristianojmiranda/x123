#!/bin/bash
source env.sh

if [[ $# -eq 0 ]] ; then
	echo "Inform resource name. Like: deploy_k8s <resource>"
	echo "resources available:"
	ll _RESOURCES | grep yaml | grep -v compose | awk '{print $9}'
fi

kube_info
source confirm

kk apply -f _RESOURCES/$1.yaml
