#!/bin/bash
source env.sh
echo -e "${F_YELLOW}========================================================${RESET}"
kk get nodes -o wide | head -n 3
echo
kk cluster-info
echo -e "${F_YELLOW}========================================================${RESET}"
echo
